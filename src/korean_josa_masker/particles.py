"""한국어 조사(助詞) 상수.

이름 뒤에 조사가 '띄어쓰기 없이' 붙는다("홍길동은"). 영어의 word boundary(``\\b``)가
통하지 않는 이유가 이것이다. '이름 + 조사'를 잡되 흔한 음절 오탐을 막으려면
어떤 음절이 조사인지 알아야 한다.

받침 유무에 따라 형태가 갈리는 이형태(allomorph)는 둘 다 넣는다:
    은/는 · 이/가 · 을/를 · 와/과 · 으로/로 · 이랑/랑 · 이나/나 · 이라도/라도 ...
"""

PARTICLES: frozenset[str] = frozenset(
    {
        # 격조사 (case)
        "은", "는", "이", "가", "을", "를", "의",
        "에", "에게", "에게서", "에서", "께", "께서", "한테", "한테서",
        "와", "과", "랑", "이랑", "하고",
        "로", "으로", "로서", "으로서", "로써", "으로써",
        "보다", "처럼", "만큼", "같이",
        # 보조사 (auxiliary)
        "도", "만", "까지", "부터", "조차", "마저", "밖에",
        "나", "이나", "든지", "이든지", "라도", "이라도", "라고", "이라고",
        # 호격 (vocative)
        "야", "아", "여", "이여",
    }
)
"""마스킹 경계 판단에 쓰는 조사 집합. 정규식에서는 긴 조사부터 매치되도록 정렬해 쓴다."""


COPULA: frozenset[str] = frozenset(
    {
        "입니다", "입니까",  # 홍길동입니다 (입 시작이라 격조사로 안 잡힘)
        "예요",              # 철수예요 (모음 뒤 이에요의 이-탈락형)
        "였",                # 철수였다 (모음 뒤 이었다의 이-탈락형)
    }
)
"""서술격 조사(이다) 형태 중 첫 음절이 격조사가 아니라 기존 경계로 못 잡히는 것.

`이` 시작 형태(이다, 이에요, 이었다)는 격조사 `이`가 이미 경계라 자동 처리됨. 누락은
`입니다`/`입니까`(입 시작), 모음 뒤 이-탈락형 `예요`(이에요)와 `였`(이었다)뿐. `다`(이다
이-탈락형)는 흔한 종결어미와 겹쳐 오탐 위험이 커서 제외. 이 집합은 경계 판단 전용이라
keep_particle=False 여도 소거하지 않고 서술어로 보존."""
